# Mock 模板库集成指南\n\n## 概述\n\n本指南说明如何在现有的 mock-data.ts 中集成新的 mock-templates.ts，实现「同一优势组合在不同场景下产生不同的回答」。\n\n---\n\n## 问题背景\n\n### 现状\n当前 `mock-data.ts` 的 `generateMockResult` 函数：\n- 只有 2 个特殊案例（信息过载、责任过载）\n- 其他所有优势组合都使用 `generateGenericExplain/Decide`\n- 结果是无论输入什么优势组合，输出都很相似\n\n### 目标\n利用 `mock-templates.ts` 中的模板库，根据优势组合的特征，路由到不同的内容生成器，实现多样化的 mock 数据。\n\n---\n\n## 集成步骤\n\n### 第一步：导入新的模板库\n\n在 `mock-data.ts` 顶部添加：\n\n```typescript\nimport {\n  selectTemplateFamily,\n  analyzeComboFeatures,\n  TEMPLATE_FAMILIES,\n  type TemplateFamilyKey,\n  describeComboFeature,\n} from './mock-templates';\nimport { STRENGTH_PROFILES } from './strength-profiles';\n```\n\n### 第二步：创建新的模板生成函数\n\n在 `mock-data.ts` 中添加以下函数（替代 `generateGenericExplain` / `generateGenericDecide`）：\n\n```typescript\n/**\n * 基于优势组合特征生成个性化解释页\n * 这是新的默认生成器，将替代 generateGenericExplain\n */\nfunction generatePersonalizedExplain(\n  scenario: ScenarioId | string,\n  strengths: StrengthId[] | string[],\n  confusion: string\n): ExplainData {\n  const strengthIds = strengths as StrengthId[];\n  const profiles = getStrengthDetails(strengthIds);\n  const strengthNames = getStrengthNames(profiles);\n  \n  // 获取优势配置信息（用于特征分析）\n  const profilesWithDomain = strengthIds.map(id => {\n    const profile = STRENGTH_PROFILES[id];\n    return {\n      id,\n      domain: profile?.domain || 'unknown',\n    };\n  });\n  \n  // 分析优势组合特征\n  const features = analyzeComboFeatures(strengthIds, profilesWithDomain);\n  const templateFamily = selectTemplateFamily(strengthIds, profilesWithDomain, confusion);\n  \n  // 调试输出（可选）\n  if (process.env.DEBUG_MOCK === 'true') {\n    console.log(`[Mock] 组合特征分析:\\n${describeComboFeature(features)}`);\n    console.log(`[Mock] 选择模板族: ${templateFamily}`);\n  }\n  \n  // 获取对应的模板\n  const templates = TEMPLATE_FAMILIES[templateFamily];\n  const problemFocus = confusion.slice(0, 20);\n  \n  // 根据模板生成内容\n  const strengthManifestations = profiles.slice(0, 3).map(profile => ({\n    strengthId: profile.name,\n    behaviors: buildStrengthBehavior(profile, confusion),\n  }));\n  \n  const strengthInteractions = templates.explain.strengthInteractionsPattern(\n    strengthNames.slice(0, 2)\n  );\n  \n  const blindspots = templates.explain.blindspotsPattern(problemFocus);\n  const summary = templates.explain.summaryPattern();\n  \n  return {\n    strengthManifestations,\n    strengthInteractions,\n    blindspots,\n    summary,\n  };\n}\n\n/**\n * 基于优势组合特征生成个性化判定页\n * 这是新的默认生成器，将替代 generateGenericDecide\n */\nfunction generatePersonalizedDecide(\n  scenario: ScenarioId | string,\n  strengths: StrengthId[] | string[],\n  confusion: string\n): DecideData {\n  const strengthIds = strengths as StrengthId[];\n  const profiles = getStrengthDetails(strengthIds);\n  const strengthNames = getStrengthNames(profiles);\n  \n  // 获取优势配置信息\n  const profilesWithDomain = strengthIds.map(id => {\n    const profile = STRENGTH_PROFILES[id];\n    return {\n      id,\n      domain: profile?.domain || 'unknown',\n    };\n  });\n  \n  // 分析优势组合特征\n  const features = analyzeComboFeatures(strengthIds, profilesWithDomain);\n  const templateFamily = selectTemplateFamily(strengthIds, profilesWithDomain, confusion);\n  \n  // 获取对应的模板\n  const templates = TEMPLATE_FAMILIES[templateFamily];\n  const firstStrength = strengthNames[0] || '优势';\n  const secondStrength = strengthNames[1] || '优势';\n  \n  // 根据模板生成路径逻辑\n  const pathLogic = templates.decide.pathDecisionLogic(firstStrength, secondStrength);\n  \n  // 根据模板决定路径判定\n  let pathDecision = PathDecision.NARROW; // 默认\n  if (templateFamily === 'RELATIONSHIP' || templateFamily === 'CONFLICTED') {\n    pathDecision = PathDecision.REFRAME;\n  } else if (templateFamily === 'EXECUTION' && features.isDispersedProne) {\n    pathDecision = PathDecision.NARROW;\n  }\n  \n  return {\n    pathDecision,\n    problemFocus: confusion.slice(0, 20),\n    reframedInsight: generateReframedInsight(confusion),\n    pathLogic,\n    pathReason: `你的「${firstStrength}」和「${secondStrength}」优势组合，在当前情境下正在${templates.explain.summaryPattern()}`,\n    doMore: generateDoMoreFromTemplate(templates, strengthNames, confusion),\n    doLess: generateDoLessFromTemplate(templates, strengthNames),\n    boundaries: generateBoundariesFromTemplate(templateFamily, strengthNames),\n    checkRule: generateCheckRuleFromTemplate(templateFamily, confusion),\n  };\n}\n```\n\n### 第三步：修改 `generateMockResult` 函数\n\n在 `generateMockResult` 中，修改下面这部分：\n\n```typescript\n// 旧代码（删除）\nlet explainData: ExplainData;\nif (isInfoOverload) {\n  explainData = generateExplainInfoOverload(scenario, strengths as string[]);\n} else if (isResponsibilityOverload) {\n  explainData = generateExplainResponsibilityOverload(scenario, strengths);\n} else {\n  explainData = generateGenericExplain(scenario, strengths, confusion);\n}\n\nlet decideData: DecideData;\nif (isInfoOverload) {\n  decideData = generateDecideInfoOverload(scenario, strengths as string[]);\n} else if (isResponsibilityOverload) {\n  decideData = generateDecideResponsibilityOverload(scenario, strengths);\n} else {\n  decideData = generateGenericDecide(scenario, strengths, confusion);\n}\n\n// 新代码（替换为）\nlet explainData: ExplainData;\nif (isInfoOverload) {\n  explainData = generateExplainInfoOverload(scenario, strengths as string[]);\n} else if (isResponsibilityOverload) {\n  explainData = generateExplainResponsibilityOverload(scenario, strengths);\n} else {\n  // 使用新的个性化生成器\n  explainData = generatePersonalizedExplain(scenario, strengths as StrengthId[], confusion);\n}\n\nlet decideData: DecideData;\nif (isInfoOverload) {\n  decideData = generateDecideInfoOverload(scenario, strengths as string[]);\n} else if (isResponsibilityOverload) {\n  decideData = generateDecideResponsibilityOverload(scenario, strengths);\n} else {\n  // 使用新的个性化生成器\n  decideData = generatePersonalizedDecide(scenario, strengths as StrengthId[], confusion);\n}\n```\n\n### 第四步：实现辅助生成函数\n\n添加以下辅助函数来根据模板生成具体内容：\n\n```typescript\n/**\n * 从模板生成 doMore 建议\n */\nfunction generateDoMoreFromTemplate(\n  templates: any,\n  strengthNames: string[],\n  confusion: string\n): Array<{ action: string; timing: string; criteria: string; consequence: string }> {\n  const firstStrength = strengthNames[0];\n  const focusRef = `「${confusion.slice(0, 15)}」`;\n  \n  return [\n    {\n      action: `用「${firstStrength}」的正向能力来处理${focusRef}`,\n      timing: '今天下班前',\n      criteria: '至少推进一个明确的小步骤',\n      consequence: '否则你会继续陷入现有的循环',\n    },\n    {\n      action: `识别「${strengthNames[1] || firstStrength}」在这个问题中的作用`,\n      timing: '明天上午',\n      criteria: '列出这个优势在当前困境中的 3 个可能用途',\n      consequence: '否则你无法充分激活所有的优势资源',\n    },\n    {\n      action: '设定一个明确的验证标准',\n      timing: '今天下班前',\n      criteria: '「我会认为这个问题被解决的标志是...」',\n      consequence: '没有标准就无法判断是否在推进',\n    },\n  ];\n}\n\n/**\n * 从模板生成 doLess 建议\n */\nfunction generateDoLessFromTemplate(\n  templates: any,\n  strengthNames: string[]\n): Array<{ action: string; replacement: string; timing: string }> {\n  return [\n    {\n      action: `停止过度使用「${strengthNames[0]}」`,\n      replacement: `转而使用「${strengthNames[1] || '其他优势'}」`,\n      timing: '从下一个情境开始',\n    },\n    {\n      action: '不再试图完美地处理',\n      replacement: '接受\"足够好\"的解决方案并立刻推进',\n      timing: '立即',\n    },\n  ];\n}\n\n/**\n * 从模板特征生成责任边界\n */\nfunction generateBoundariesFromTemplate(\n  templateFamily: TemplateFamilyKey,\n  strengthNames: string[]\n): Array<{ responsibleFor: string; notResponsibleFor: string }> {\n  if (templateFamily === 'RELATIONSHIP') {\n    return [\n      {\n        responsibleFor: '理解他人的需要',\n        notResponsibleFor: '满足他人的所有期待',\n      },\n      {\n        responsibleFor: '维持关系',\n        notResponsibleFor: '承担关系中的所有成本',\n      },\n    ];\n  } else if (templateFamily === 'EXECUTION') {\n    return [\n      {\n        responsibleFor: '付诸行动',\n        notResponsibleFor: '保证结果完美',\n      },\n      {\n        responsibleFor: '快速推进',\n        notResponsibleFor: '同时处理所有方向',\n      },\n    ];\n  } else if (templateFamily === 'STRATEGIC') {\n    return [\n      {\n        responsibleFor: '做出最优判断',\n        notResponsibleFor: '预测所有可能性',\n      },\n      {\n        responsibleFor: '信息充分时决策',\n        notResponsibleFor: '等待完全确定',\n      },\n    ];\n  }\n  \n  // 默认边界\n  return [\n    {\n      responsibleFor: '做出当前最优判断',\n      notResponsibleFor: '保证这是唯一的答案',\n    },\n    {\n      responsibleFor: '用优势发挥最大价值',\n      notResponsibleFor: '补齐所有短板',\n    },\n  ];\n}\n\n/**\n * 生成自检规则\n */\nfunction generateCheckRuleFromTemplate(\n  templateFamily: TemplateFamilyKey,\n  confusion: string\n): string {\n  const focus = confusion.slice(0, 15);\n  \n  switch (templateFamily) {\n    case 'RELATIONSHIP':\n      return `判断标准：今天是否对一个不合理的请求说了'不'，且没有产生内疚？`;\n    case 'EXECUTION':\n      return `判断标准：今天的行动是否朝着一个清晰的目标推进，而不是四面八方同时发力？`;\n    case 'STRATEGIC':\n      return `判断标准：今天是否用足够的信息做了一个判断，然后立刻进入验证而不是继续思考？`;\n    case 'CONFLICTED':\n      return `判断标准：今天是否在不同阶段成功地激活了不同的优势，而不是让它们互相拉扯？`;\n    default:\n      return `判断标准：今天是否在「${focus}」这件事上取得了清晰的进展？`;\n  }\n}\n```\n\n---\n\n## 使用效果对比\n\n### 改进前\n\n所有优势组合都返回相同的通用模板，例如：\n\n```\n优势表现：\n- 责任：你会默认所有事都\"不能出错、不能放手\"，觉得拒绝就是不负责任。\n- 战略：你会在任务尚未成型前，先试图把所有变量想清楚。\n\n互动：当你的\"责任\"遇到\"战略\"，你们在用一种互相消耗的方式配合。\n\n总结：你这组优势的核心模式：用准备代替选择。\n```\n\n### 改进后\n\n不同优势组合返回不同的模板内容：\n\n**组合 1：执行力 + 纪律 + 责任（执行力主导）**\n\n```\n互动：当你的\"执行力\"遇到\"纪律\"，你会自动启动行动模式，几乎不给自己思考的空间。\n      你一旦决定了就要立刻做，如果无法立刻行动就会产生焦虑。\n\n总结：你这组优势的核心模式：用行动代替思考。\n```\n\n**组合 2：和谐 + 体谅 + 适应（关系建立主导）**\n\n```\n互动：当你的\"和谐\"遇到\"体谅\"，你会进入\"了解他人 → 适应他人 → 为他人考虑\"的自动化流程。\n      你对别人的需要总是比对自己的需要更敏感。\n\n总结：你这组优势的核心模式：用适应代替设立边界。\n```\n\n**组合 3：分析 + 学习 + 战略（战略思维主导）**\n\n```\n互动：当你的\"分析\"遇到\"学习\"，你会进入\"收集信息 → 分析对比 → 发现新维度\"的循环。\n      你在等待一个永远不会到来的\"完全确定的答案\"。\n\n总结：你这组优势的核心模式：用思考代替决策。\n```\n\n---\n\n## 配置和调试\n\n### 启用调试输出\n\n在环境变量中设置：\n\n```env\nDEBUG_MOCK=true\n```\n\n然后查看控制台输出，会显示：\n\n```\n[Mock] 组合特征分析：\n优势组合分析：\n- 跨领域：否\n- 领域冲突：是\n- 执行力占比：50%\n- 关系占比：30%\n- 战略占比：20%\n...\n\n[Mock] 选择模板族: EXECUTION\n```\n\n### 验证集成\n\n1. 在本地测试：\n   ```bash\n   DEBUG_MOCK=true npm run dev\n   ```\n\n2. 尝试不同的优势组合，验证输出是否不同\n\n3. 检查控制台日志确认模板选择是否合理\n\n---\n\n## 下一步改进\n\n### 短期\n1. 增加更多优势组合的模板（如影响力主导、具体的冲突对）\n2. 根据用户反馈调整模板内容\n3. 增加场景感知（同一优势组合在不同场景下的不同表现）\n\n### 中期\n1. 创建组合特征数据库（CSV / JSON），便于非技术人员维护\n2. 实现 A/B 测试，对比 AI 生成 vs Mock 生成的质量\n3. 建立模板版本管理，支持快速迭代\n\n### 长期\n1. 基于用户反馈的自动化模板优化\n2. 支持多语言模板\n3. 个性化模板（基于用户的特定场景和历史输入）\n\n---\n\n## 常见问题\n\n### Q: 如何添加新的优势组合模板？\n\nA: 在 `mock-templates.ts` 中：\n1. 创建新的 `XXX_TEMPLATES` 对象\n2. 实现 `explain` 和 `decide` 模式\n3. 在 `selectTemplateFamily` 中添加路由逻辑\n4. 在 `TEMPLATE_FAMILIES` 中导出\n\n### Q: 模板内容太生硬了，如何改进？\n\nA: 每个模板中的 `Pattern` 函数都可以参数化。例如：\n```typescript\nstrengthlnteractionPattern: (names: string[], domain: string) => \n  `当你的「${names[0]}」遇到「${names[1]}」在${domain}领域...`\n```\n\n### Q: 如何避免 Mock 和 AI 内容冲突？\n\nA: 保持模板库和 AI prompt 的结构一致，例如都使用\"你的核心模式\"这样的框架。Mock 的模板应该\"弱化\"版本的 AI 内容，而非完全独立的内容。\n\n---\n\n**最后更新**：2026-01-20\n"